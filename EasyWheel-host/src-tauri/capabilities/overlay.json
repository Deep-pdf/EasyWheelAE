{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capability set for the EasyWheel overlay window.",
  "windows": ["overlay"],
  "permissions": [
    "core:default"
  ]
}
