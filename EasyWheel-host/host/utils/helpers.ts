/**
 * helpers.ts
 *
 * General-purpose utility functions that do not belong to a specific domain.
 *
 * Functions (Phase 2+):
 * - Deep equality checks for plain objects
 * - Safe JSON parse with a fallback value
 * - Generating stable unique identifiers for overlay elements
 * - Debounce and throttle wrappers for event handlers
 */
