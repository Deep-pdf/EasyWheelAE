/**
 * Direction
 *
 * Represents a cardinal or ordinal direction on the radial wheel.
 *
 * Phase 2+ usage:
 * - Maps angular mouse position to a named wheel segment.
 * - Used by geometry utilities to compute which slice is active.
 */
export interface Direction {}
