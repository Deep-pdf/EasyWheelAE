/**
 * Settings
 *
 * Persistent user configuration for EasyWheel Host.
 *
 * Phase 2+ fields (indicative):
 * - hotkey: the global keyboard shortcut that triggers the wheel
 * - wheelRadius: pixel radius of the rendered radial wheel
 * - actions: ordered list of Action configurations per wheel position
 * - launchOnStartup: whether to register with Windows Task Scheduler
 */
export interface Settings {}
